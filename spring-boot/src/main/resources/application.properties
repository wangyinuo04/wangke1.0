# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/new-wangke?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=723102517

# 应用配置
spring.application.name=demo-app
server.port=8088
spring.devtools.restart.enabled=true
spring.mvc.static-path-pattern=/images/**

# 文件上传配置
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Spring MVC配置
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# MyBatis Plus配置
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl
mybatis-plus.mapper-locations=classpath*:mapper/**/*.xml
mybatis-plus.type-aliases-package=com.example.demo_app.entity
mybatis-plus.configuration.map-underscore-to-camel-case=true

# 日志配置
logging.level.com.example.demo_app.mapper=DEBUG

# 邮件配置
spring.mail.host=smtp.qq.com
spring.mail.port=465
spring.mail.username=3100677323@qq.com
spring.mail.password=thgdacieteojdffb
spring.mail.protocol=smtp
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=false
spring.mail.properties.mail.smtp.socketFactory.port=465

# 解决循环依赖问题
spring.main.allow-circular-references=true

# 文件存储配置
file.upload-dir=uploads/resources
file.allowed-extensions=.pdf,.ppt,.pptx,.doc,.docx,.txt,.mp4,.avi,.mov,.mp3,.wav,.jpg,.jpeg,.png

# 静态资源访问路径
spring.web.resources.static-locations=classpath:/static/,classpath:/uploads/,file:src/main/resources/uploads/